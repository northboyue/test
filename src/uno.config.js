import { defineConfig, presetUno, presetAttributify } from "unocss";
export default defineConfig({
    rules: [],
    presets: [
        presetUno(),
        presetAttributify(),
    ],
    shortcuts: {},
    theme: {},
});